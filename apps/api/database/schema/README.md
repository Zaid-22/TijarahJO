# Database Schema

Canonical base schema source files live in this folder.

- `BASE_SCHEMA.sql`: source of truth for table/constraint baseline

This folder must only contain schema definitions. Do not add:
- stored procedures
- seed data
- environment-specific data fixes
- generated bundle artifacts

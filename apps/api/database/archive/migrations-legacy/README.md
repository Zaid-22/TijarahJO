# Legacy Migrations Archive

This folder stores historical `Tb*` and pre-canonical migration scripts.

- These files are **not** included in active bundle generation.
- Active bundle inputs are defined explicitly in `../build_sql_bundles.sh`.
- Use these files only for manual legacy upgrade analysis or forensic comparison.

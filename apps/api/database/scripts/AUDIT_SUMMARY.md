# SQL Audit Summary

Audit date: 2026-02-19

Regenerate this audit snapshot with:

```bash
make sql-audit
```

## Inventory Snapshot

- Total SQL files (`database/schema` + `database/scripts`): `29`
- Active source SQL files (`schema`, `migrations`, `procedures`, `seeds`): `21`
- Archived SQL files (`archive`): `0`
- Consolidated generated bundles (`bundles`): `7`

## Active Source Categorization

- `../schema/`
  - `BASE_SCHEMA.sql` (canonical base schema source)
- `migrations/`
  - `14` ordered, append-only versioned migration scripts (`VYYYYMMDDHHMM__description.sql`)
- `procedures/`
  - `CANONICAL_STORED_PROCEDURES.sql` (single canonical runtime procedure source)
- `seeds/`
  - `5` baseline/dev/test seed scripts

## Consolidated Output Categories

Generated by `build_sql_bundles.sh`:

- `apps/api/database/bundles/schema.sql`
- `apps/api/database/bundles/migrations.sql`
- `apps/api/database/bundles/procedures.sql`
- `apps/api/database/bundles/seed_data.sql`
- `apps/api/database/bundles/seed_dev.sql`
- `apps/api/database/bundles/seed_test.sql`
- `apps/api/database/bundles/master.sql`

## Redundancy Findings

- Active sources contain no duplicate stored procedure names.
- Active sources contain no duplicate index names.
- Full `database` tree contains no duplicate full-file hashes.

## Optimization Actions Applied

- Canonical base schema source moved to `database/schema/BASE_SCHEMA.sql`.
- Stored procedure source consolidated into `procedures/CANONICAL_STORED_PROCEDURES.sql`.
- Test cleanup scripts consolidated into `maintenance/CLEANUP_TEST_DATA.sql`.
- `apps/api/database/bundles/indexes.sql` removed from generation.
- `./scripts/bootstrap_db.sh` now applies:
  1. `apps/api/database/bundles/master.sql` (`base schema + migrations + procedures`)
  2. `apps/api/database/bundles/seed_data.sql` (baseline only)
  3. `apps/api/database/bundles/seed_dev.sql` only with `--with-dev-seeds`
  4. `apps/api/database/bundles/seed_test.sql` only with `--with-test-seeds`
  5. search normalization migration `V202602191020__add_normalized_search_columns_and_indexes.sql` for prefix-indexed search paths

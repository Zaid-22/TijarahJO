#!/usr/bin/env bash

# Query duration thresholds (milliseconds) used by run_performance_baseline.sh.
# Keep these in source control as baseline budgets; override via env vars in CI/staging if needed.

PERF_MAX_FEED_MS=400
PERF_MAX_SEARCH_MS=500
PERF_MAX_TOP_SELLERS_MS=450
PERF_MAX_CHAT_HISTORY_MS=300
